<!DOCTYPE html>
<% if signed_in? %>
	<table class="front" summary="Pour visiteur identifie">
		<tr>
			<h1>
				 <%= $evenement_titre  %> 
			</h1>
			<td class="main">
				<h3 class="Commentaires">Commentaire</h3>
				<%= render 'shared/commentaire_form' %>
				<%= render 'shared/feed_commentaire' %>
			</td>
			<td class="sidebar round">
				<%= render 'shared/user_info' %>
				<%= render 'shared/statsfriends' %>
			</td>
		</tr>
	</table>
<% end %>