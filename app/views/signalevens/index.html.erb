<% if signed_in? && current_user.admin? %>
	<table class="front" summary="Pour visiteur identifie">
		<tr>
			<td class="main">
				<% $index_even = 1 %>
			<br/><br/><br/>	<h2>Tous les signalevens</h2>
				<ul class="signaleven">
					<%= render @signalevens %>
				</ul>
				<%= will_paginate %>
			</td>
			<td class="sidebar round">
				<br/><br/><br/><%= render 'shared/user_info' %>
				<%= render 'shared/statsfriends' %>
			</td>
		</tr>
	</table>
 <% end %>
