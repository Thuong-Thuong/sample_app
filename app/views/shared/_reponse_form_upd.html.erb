<%= form_for([$comment, @reponse])  do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
 	<div class="field">
    	<%= f.text_area :reponse %>
	</div>
	<div class="actions">
		<%= f.submit "Modifier", :class => 'buttons' %>
	</div>
<% end %>
