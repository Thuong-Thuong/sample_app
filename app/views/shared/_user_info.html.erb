<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <%= gravatar_pour current_user, :size => 50 %>
    <!--
   <span class="user_name">
    -->
      </strong> <%= current_user.nom %>
    </span>
    <span class="sidebar _round">
    <!-- 
 <%= current_user.email %> 
   -->
   </strong> <%= current_user.email%><br />
    </span>

    <span class="sidebar round">
<strong>Messages</strong> <%= current_user.microposts.count %>
    </span>
  </a>
</div>