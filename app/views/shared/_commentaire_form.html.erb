<%= form_for @commentaire do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <div class="field">
    	<%= f.text_area :commentaire %>
	</div>
	<div class="actions">
		<%= f.submit "Soumettre", :class => 'buttons' %>
	</div>
<% end %>
