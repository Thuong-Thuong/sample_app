<%= form_for([$comment, $comment.reponses.build])  do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
 	<div class="field">
    		<%= f.text_area :reponse %>
	</div>
	<div class="actions">
		<%= f.submit "Envoyer", :class => 'buttons' %>
	</div>
<% end %>







