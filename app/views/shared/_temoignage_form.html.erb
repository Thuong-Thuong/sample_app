<%= form_for @temoignage do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <div class="field">
    	<%= f.text_area :temoignage %>
	</div>
	<div class="actions">
		<%= f.submit "Soumettre", :class => 'round' %>
	</div>
<% end %>
